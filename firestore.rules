rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // Helper function to check if the user owns the document
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }
    
    // Rules for prompts collection
    match /prompts/{promptId} {
      // Allow get if the document belongs to the authenticated user
      allow get: if isOwner(resource.data.userId);
      
      // Allow list if user is authenticated and querying their own documents
      allow list: if request.auth != null && request.query.limit <= 50;
      
      // Allow create if user is authenticated and sets themselves as owner
      allow create: if isAuthenticated() 
        && request.resource.data.userId == request.auth.uid;
      
      // Allow update if user owns the document and isn't changing ownership
      allow update: if isOwner(resource.data.userId)
        && request.resource.data.userId == resource.data.userId;
      
      // Allow delete if user owns the document
      allow delete: if isOwner(resource.data.userId);
    }
    
    // Rules for users collection (for API keys)
    match /users/{userId} {
      // Users can only read and write their own data
      allow read, write: if isOwner(userId);
      
      // Rules for custom OpenRouter models subcollection
      match /customOpenRouterModels/{modelId} {
        // Allow read operations with query limit
        allow read: if isOwner(userId) && request.query.limit <= 50;
        // Allow write operations for the owner
        allow write: if isOwner(userId);
      }
    }
    
    // Rules for workspaces collection
    match /workspaces/{workspaceId} {
      // Allow get if the document belongs to the authenticated user
      allow get: if isOwner(resource.data.userId);
      
      // Allow list if user is authenticated and querying their own documents
      allow list: if request.auth != null && request.query.limit <= 50;
      
      // Allow create if user is authenticated and sets themselves as owner
      allow create: if isAuthenticated() 
        && request.resource.data.userId == request.auth.uid;
      
      // Allow update if user owns the document and isn't changing ownership
      allow update: if isOwner(resource.data.userId)
        && request.resource.data.userId == resource.data.userId;
      
      // Allow delete if user owns the document
      allow delete: if isOwner(resource.data.userId);
    }
    
    // Rules for threads collection
    match /threads/{threadId} {
      // Allow get if the document belongs to the authenticated user
      allow get: if isOwner(resource.data.userId);
      
      // Allow list if user is authenticated and querying their own documents
      allow list: if request.auth != null && request.query.limit <= 50;
      
      // Allow create if user is authenticated and sets themselves as owner
      allow create: if isAuthenticated() 
        && request.resource.data.userId == request.auth.uid;
      
      // Allow update if user owns the document and isn't changing ownership
      allow update: if isOwner(resource.data.userId)
        && request.resource.data.userId == resource.data.userId;
      
      // Allow delete if user owns the document
      allow delete: if isOwner(resource.data.userId);
    }
  }
}